{
  "name": "hermes-e2e-web",
  "private": true,
  "version": "0.0.1",
  "description": "Playwright end-to-end tests for the Hermes web demo (user chat + admin dashboard login).",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0"
  }
}
